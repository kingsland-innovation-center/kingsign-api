export enum FieldType {
  SIGNATURE = 'signature',
  TEXT = 'text',
  CHECKBOX = 'checkbox',
  DATE = 'date'
}

export enum DocumentStatus {
  PENDING = 'pending',
  SIGNED = 'signed',
  COMPLETED = 'completed'
} 